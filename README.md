# Sonical API

## Dependencies
* Python >= 3.6
* PostgreSQL >= 9.6.0

## Setup
`$ mkdir ~/.venv && cd ~/.venv`
`$ python3.6 -m venv sonical && source sonical/bin/activate`
`$ pip install -r requirements.txt`

## Test
`$ ./bin/do test`
